<UserControl x:Class="SenSÉ.Atelier.UI.Palette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource FondPanneauBrush}" BorderBrush="{StaticResource BordureBrush}" BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Style="{StaticResource EnteteSection}" Text="Palette" Margin="8,8,8,4" />
            <Button Grid.Row="1" x:Name="BtnNouveauCustom" Content="+ Noeud custom" Click="BtnNouveauCustom_Click" Margin="8,0,8,4" />
            <ListView x:Name="Liste" Grid.Row="2" Background="Transparent" BorderThickness="0"
                      Foreground="{StaticResource TexteBrush}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.ContainerStyle>
                            <Style TargetType="GroupItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <Expander IsExpanded="True" Background="Transparent" Foreground="{StaticResource TexteSecondaireBrush}" Margin="0,4,0,0">
                                                <Expander.Header>
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="11" />
                                                </Expander.Header>
                                                <ItemsPresenter />
                                            </Expander>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </GroupStyle.ContainerStyle>
                    </GroupStyle>
                </ListView.GroupStyle>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Padding" Value="0" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,6" BorderThickness="0,0,0,1" BorderBrush="#2D2D40" Background="Transparent"
                                PreviewMouseLeftButtonDown="Item_PreviewMouseLeftButtonDown" MouseLeftButtonUp="Item_MouseLeftButtonUp" Cursor="Hand">
                            <StackPanel>
                                <TextBlock Text="{Binding Nom}" Foreground="{StaticResource TexteBrush}" FontSize="12" />
                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource TexteSecondaireBrush}" FontSize="10" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</UserControl>