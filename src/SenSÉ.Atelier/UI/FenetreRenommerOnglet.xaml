<Window x:Class="SenSÉ.Atelier.UI.FenetreRenommerOnglet"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Renommer le graphe" Height="160" Width="420" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="Label" Text="Nom du graphe :" Margin="0,0,0,8" />
        <TextBox Grid.Row="1" x:Name="Champ" Margin="0,0,0,16" KeyDown="Champ_KeyDown" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnAnnuler" Content="Annuler" Click="BtnAnnuler_Click" Width="90" Margin="0,0,8,0" />
            <Button x:Name="BtnOK" Content="OK" Click="BtnOK_Click" IsDefault="True" Width="90" />
        </StackPanel>
    </Grid>
</Window>
