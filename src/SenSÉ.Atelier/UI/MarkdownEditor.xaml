<UserControl x:Class="SenSÉ.Atelier.UI.MarkdownEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="root">
    <UserControl.Resources>
        <Style x:Key="BtnMdTool" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource TexteBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,3" />
            <Setter Property="Margin" Value="0,0,2,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="11" />
        </Style>
        <Style x:Key="BtnMdToolActif" TargetType="Button" BasedOn="{StaticResource BtnMdTool}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="#1E1E2E" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </UserControl.Resources>
    <Border BorderBrush="{StaticResource BordureBrush}" BorderThickness="1" CornerRadius="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{StaticResource FondPanneauBrush}" BorderBrush="{StaticResource BordureBrush}" BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal" Margin="4,3">
                    <Button x:Name="BtnEdit"    Style="{StaticResource BtnMdToolActif}" Content="Edit"    Click="BtnEdit_Click" />
                    <Button x:Name="BtnPreview" Style="{StaticResource BtnMdTool}"      Content="Preview" Click="BtnPreview_Click" />
                    <Border Width="1" Background="{StaticResource BordureBrush}" Margin="6,3" />
                    <Button x:Name="BtnInserer" Style="{StaticResource BtnMdTool}" Content="Inserer un fichier..." Click="BtnInserer_Click" />
                </StackPanel>
            </Border>
            <TextBox Grid.Row="1" x:Name="ZoneEdit"
                     AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas, Courier New" FontSize="12"
                     BorderThickness="0" Padding="6"
                     TextChanged="ZoneEdit_TextChanged" />
            <ScrollViewer Grid.Row="1" x:Name="ZonePreview" Visibility="Collapsed"
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          Padding="8,6">
                <StackPanel x:Name="ApercuStack" />
            </ScrollViewer>
            <TextBlock Grid.Row="2" x:Name="Status" FontSize="10"
                       Foreground="{StaticResource TexteSecondaireBrush}"
                       Margin="6,2" Text="0 car. / 0 lign." />
        </Grid>
    </Border>
</UserControl>
