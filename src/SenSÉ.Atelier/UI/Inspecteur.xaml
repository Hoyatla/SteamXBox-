<UserControl x:Class="SenSÉ.Atelier.UI.Inspecteur"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource FondPanneauBrush}" BorderBrush="{StaticResource BordureBrush}" BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Style="{StaticResource EnteteSection}" Text="Inspecteur" />
            <StackPanel Grid.Row="1" Margin="8,0,8,4" x:Name="EnteteNoeud">
                <TextBlock x:Name="Titre" Text="— Aucun noeud —" FontSize="14" FontWeight="SemiBold" />
                <TextBlock x:Name="Description" Foreground="{StaticResource TexteSecondaireBrush}" TextWrapping="Wrap" FontSize="11" />
            </StackPanel>
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="Form" Margin="8" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>