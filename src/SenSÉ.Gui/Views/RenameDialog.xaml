<Window x:Class="SenSÉ.Gui.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Renommer la manette"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="NoResize"
        Background="Transparent">

    <Border Background="{StaticResource BgDarkBrush}" CornerRadius="10" Padding="18"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="Renommer la manette" FontSize="16" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Ce nom reste attaché à cette manette, à chaque reconnexion."
                       FontSize="11" TextWrapping="Wrap" Margin="0,4,0,0"
                       Foreground="{StaticResource TextDimBrush}"/>

            <TextBox x:Name="NameBox" Style="{StaticResource TextBoxDark}" Margin="0,14,0,16"
                     MaxLength="40" KeyDown="NameBox_KeyDown"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Annuler" Click="Cancel_Click" Padding="14,6" Margin="0,0,8,0"
                        Style="{StaticResource SecondaryButton}"/>
                <Button Content="OK" Click="Ok_Click" Padding="18,6"
                        Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
