<Project>
  <PropertyGroup>
    <Version>0.5.1</Version>
    <AssemblyVersion>0.5.1</AssemblyVersion>
    <FileVersion>0.5.1</FileVersion>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <Deterministic>true</Deterministic>
    <!-- Proprietary. PackageLicenseExpression only accepts recognised SPDX identifiers, so the
         terms live in LICENSE at the root and are pointed at from here. -->
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <Copyright>Copyright (c) 2026 Hoyatla. Tous droits réservés.</Copyright>
    <Company>Hoyatla</Company>
    <Product>SenSÉ</Product>
  </PropertyGroup>
</Project>
