<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>HidProbe</AssemblyName>
    <RootNamespace>HidProbe</RootNamespace>
    <!-- Deliberately not part of the solution: a read-only bench instrument, not product code. -->
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\Sc2Xboxed.Windows\Sc2Xboxed.Windows.csproj" />
    <PackageReference Include="HidSharp" Version="2.6.4" />
  </ItemGroup>
</Project>